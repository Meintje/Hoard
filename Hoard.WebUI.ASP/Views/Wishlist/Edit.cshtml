@model Hoard.WebUI.Services.ViewModels.Wishlist.CreateUpdate.WishlistUpdateViewModel

@{
    ViewData["Title"] = "Edit";
}

<div class="row mb-3">
    <div class="ml-0 mr-auto" style="margin-top: -20px;">
        <a asp-action="Index" class="btn btn-success btn-lg">< Index</a>
    </div>
</div>

<div class="row mb-2">
    <div>
        <h1>Editing @Html.DisplayFor(model => model.Title)</h1>
    </div>
</div>

<div class="row card-row">
    <div class="col-lg-12">
        <div class="card">
            <div class="card-body">
                <form asp-action="Edit" autocomplete="off">
                    <partial name="_CreateUpdateFormPartial" />
                    <input type="hidden" asp-for="ID" />
                    <input type="hidden" asp-for="AddDate" />
                    <div class="form-group">
                        <input type="submit" value="Save" class="btn btn-success" />
                        <a asp-action="Index" class="btn btn-danger">Cancel</a>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}

